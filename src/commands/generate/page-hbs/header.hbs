<header class="page-header">
  <section>
    <a class="page-header__title-a" href="/">
      <div class="page-header__left">
        {{#if page.metadata.logoSrc}}
          <img src="{{page.metadata.logoSrc}}" alt="Documentation page logo image">
        {{/if}}

        <p>
          {{#if page.metadata.brandTitle}}
            {{{page.metadata.brandTitle}}}
          {{else}}
            Doc
          {{/if}}
        </p>
      </div>
    </a>

    <div class="page-header__right">
      {{{config.autoInclude.header}}}
    </div>
  </section>
</header>